# 经营概览 PRD

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 编号 | 06-01 |
| 版本 | V1.0 |
| 状态 | 待评审 |
| 所属模块 | 数据看板 / 经营概览 |
| 负责人角色 | 平台管理员、运营人员、销售主管 |
| 更新时间 | 2026-07-17 |

## 2. 页面概述

本页将供给、线索、带看、转化和人员活跃度置于同一经营周期中，供管理者识别城市、商户、团队或经纪人范围内的供需及转化问题。成功标准是任一数值均可解释、与下钻明细一致且同比/环比使用相同的数据范围。MVP 不计算合同、支付、佣金或结算金额，也不将“已联系”“需求确认”“已推荐”“无效”混入统一线索阶段漏斗。

## 3. 用户与权限

平台管理员可查看全平台并切换城市、商户、团队和经纪人；运营人员仅查看授权城市及其供给相关线索；销售主管仅查看所属团队及授权城市；经纪人无本页菜单，使用工作台数据概览。数据范围取账号授权范围与当前筛选的交集，城市、商户、团队、经纪人均使用不可变 ID；无指标权限的组件不渲染，不以 `0` 替代。手机号、客户姓名和联系方式不进入图表、排行或导出。

## 4. 页面入口与关联

- 菜单入口：`数据看板 > 经营概览`；工作台数据概览的“查看经营分析”可带入周期和数据范围。
- 上游：楼盘、房源、审核、商户、经纪人、团队、线索、跟进、带看记录、阶段历史及权限服务。
- 下游：点击 KPI、趋势点、对比项或排行进入房源列表、线索池、我的客户、经纪人管理或团队管理，并携带本页日期、范围、状态和维度筛选。
- 返回：下钻返回后恢复 URL 中的筛选、对比周期、排序和图表缩放状态。

## 5. 页面结构

顶部为时间、对比和数据范围栏；随后依次为供给卡片、线索转化卡片、趋势与周期对比、城市/商户/团队排行和更新时间。默认周期为近 30 日、对比上一等长周期；存量指标显示“截至查询时刻”，流量指标按所选北京时间日期闭区间聚合。图表区域固定高度，加载不改变布局；移动端按供给、线索、人员、排行顺序纵向排列。

## 6. 查询与筛选

| 条件 | 规则 |
|---|---|
| 时间范围 | 今日、近 7 日、近 30 日、自定义；默认近 30 日，最长 365 天，按北京时间自然日闭区间。 |
| 对比周期 | 上一周期、去年同期、关闭；默认上一等长周期，存量指标比较两个周期结束时刻。 |
| 城市/区域/商圈 | 多选且级联，只展示授权且启用的当前节点；停用节点仅在历史明细中可见。 |
| 商户/团队/经纪人 | 级联并受数据权限约束；选经纪人时自动限定其有效归属历史所在团队。 |
| 租售类型 | 租赁、出售、全部；出售仅展示供给和线索意向，不推导交易金额。 |

筛选、对比、图表维度和分页写入 URL；清空恢复默认近 30 日与账号默认范围。开始日期不得晚于结束日期。

## 7. 列表或内容展示

卡片显示指标名、当前值、对比值、变化率、时间口径与最后刷新时间；变化率为 `(本期-对比期)/对比期`，对比期为 0 时显示 `--`。趋势图按日展示 `OP-03` 至 `OP-08`，可按城市、商户、团队或经纪人切分；排行默认按 `OP-07` 转化数降序、同值按不可变 ID 升序，前 10 名，隐藏无权限行。空数据按“当前条件暂无数据”展示；没有对比数据时保留当前值并显示 `--`。

## 8. 详情及表单

本页无新增或编辑表单。点击指标说明、趋势点或排行行打开只读口径抽屉，显示指标字典条目、当前筛选、分子/分母、最近刷新批次、数据延迟与下钻条件；点击“查看明细”执行第 12 节对应下钻。导出仅导出当前图表聚合结果和已授权维度，不导出客户或联系方式；异步导出保存条件、行数、操作者和秒级时间。

## 9. 核心操作与流程

1. 用户设置周期和数据范围，系统校验其为权限范围的子集。
2. 系统读取已完成聚合；若聚合批次晚于刷新承诺或用户点击刷新，则按同一筛选触发查询并标识“刷新中”。
3. 用户可切换趋势维度、对比周期、排序指标或点击下钻；下钻请求必须携带指标 ID 和查询快照 ID。
4. 后端在明细页再次执行数据权限过滤；若快照过期，提示数据已更新并按当前口径重新计算。

所有查询、刷新、口径抽屉、排行查看、下钻和导出均写操作日志；本页不允许手工修改指标。

## 10. 状态与业务规则

房源前台活跃的必要条件为：`审核状态=通过`、`发布状态=已上架`、所属楼盘启用、商户可发布、无有效暂停且租赁房源可租；已出租自动排除。线索统一使用 `新建 → 待分配 → 跟进中 → 有效商机 → 带看 → 谈判 → 已转化/已流失`，公海仅是归属位置；已转化、已流失为终态。阶段、归属和人员资格均按事件发生时的历史快照归因，当前存量按查询时刻的当前状态归因。人工合并线索仅计合并目标 `lead_id`，被合并记录不重复计数。

## 11. 异常与边界

- 指标聚合失败或延迟超过承诺时显示最近成功值、刷新时间和重试，不把未知值显示为 0。
- 维度成员在周期内停用、转组或离职时，流量按事件时归属历史统计，存量按查询时资格统计；无权访问的历史成员名称显示“无权限成员”。
- 分母为 0、对比期为 0、事件时间缺失或重复事件均按第 12 节规则处理并记录数据质量计数。
- URL 篡改到未授权城市、商户、团队或经纪人时返回无权限或自动收缩到可见范围；不得通过排行名称推断被隐藏对象。
- 查询快照与下钻期间数据变化时，明细页显示当前数据刷新提示，不伪造与旧快照相等的结果。

## 12. 数据与指标

数据来自 Building、Listing、审核快照、Merchant、Broker、Team、Lead、LeadStageHistory、LeadAssignmentHistory、FollowUpRecord、ViewingRecord 与权限快照。所有时间均以北京时间（UTC+8）解释，日粒度使用 `00:00:00-23:59:59.999`，存量以查询时刻快照为准。以下为本页全部展示 KPI；“排除”同时包含逻辑删除、测试数据、无城市归属及无权范围记录。

| 指标 | 业务定义 | 计算公式（分子/分母） | 去重键 | 状态范围（含/排除） | 时间字段 | 时区 | 维度 | 刷新频率 | 下钻 |
|---|---|---|---|---|---|---|---|---|---|
| OP-01 启用楼盘数 | 查询时刻可为房源供给的楼盘存量。 | 分子：满足条件的楼盘数；分母：不适用。 | `building_id` | 含楼盘启用；排除停用、逻辑删除和无权限城市。 | `snapshot_at` | 北京时间（UTC+8） | 城市、区域、商圈、商户、楼盘类型。 | 每 5 分钟；楼盘状态事件后 60 秒内失效。 | 楼盘列表：`status=启用` 与当前范围。 |
| OP-02 活跃房源数 | 查询时刻满足前台活跃必要条件的具体房源数。 | 分子：活跃 `listing_id` 数；分母：不适用。 | `listing_id` | 含审核通过、已上架、楼盘启用、商户可发布、未暂停，租赁还须可租；排除草稿、待审核、驳回、下架、已出租、暂停。 | `snapshot_at` | 北京时间（UTC+8） | 城市、商圈、商户、经纪人、租售类型、装修、标签。 | 每 5 分钟；发布/审核/暂停事件后 60 秒内失效。 | 房源列表：上述状态与当前范围。 |
| OP-03 新增房源数 | 周期内首次创建的房源供给量，不代表已发布。 | 分子：周期内创建且未被合并的房源数；分母：不适用。 | `listing_id` | 含草稿至已出租全部非删除发布状态；排除逻辑删除、重复合并源记录。 | `listing.created_at` | 北京时间（UTC+8） | 城市、商户、楼盘、租售类型、创建人。 | 每 5 分钟；创建/合并事件后 60 秒内失效。 | 房源列表：`created_at=当前周期` 与当前范围。 |
| OP-04 新增线索数 | 周期内接入校验通过的新需求量。 | 分子：创建且有效接入 Lead 数；分母：不适用。 | 合并目标 `lead_id` | 含新建至终态所有已接入阶段；排除接入拒绝、逻辑删除、被合并源线索。 | `lead.created_at` | 北京时间（UTC+8） | 城市、来源、商户、团队、经纪人、意向楼盘/房源、租售意向。 | 每 5 分钟；创建/合并事件后 60 秒内失效。 | 线索池：`created_at=当前周期` 与当前范围。 |
| OP-05 有效商机数 | 周期内首次进入有效商机的线索量。 | 分子：首次有效商机事件数；分母：不适用。 | `lead_id` | 含阶段变为有效商机的非合并目标；排除无效接入、已合并源及无阶段证据记录。 | `lead_stage_history.occurred_at` | 北京时间（UTC+8） | 城市、来源、团队、经纪人、意向楼盘/房源。 | 每 5 分钟；阶段事件后 60 秒内失效。 | 线索池：`stage_history=有效商机首次进入` 与当前范围。 |
| OP-06 带看线索数 | 周期内首次有有效带看事实的线索量。 | 分子：首次带看成功记录 Lead 数；分母：不适用。 | `lead_id` | 含阶段进入带看且存在成功带看记录；排除取消、无效、测试及被合并源记录。 | `viewing_record.completed_at` | 北京时间（UTC+8） | 城市、来源、团队、经纪人、楼盘、房源。 | 每 5 分钟；带看保存/更正事件后 60 秒内失效。 | 我的客户：`首次成功带看时间=当前周期` 与当前范围。 |
| OP-07 转化数 | 周期内首次进入已转化终态的线索量，仅表示转化事实。 | 分子：首次转化事件 Lead 数；分母：不适用。 | `lead_id` | 含已转化；排除已流失、无效接入、被合并源及合同/支付推断事件。 | `lead_stage_history.occurred_at`（`converted_at`） | 北京时间（UTC+8） | 城市、来源、商户、团队、经纪人、楼盘、房源。 | 每 5 分钟；转化/合并事件后 60 秒内失效，日终 01:00 回算。 | 线索池：`stage=已转化`、`converted_at=当前周期` 与当前范围。 |
| OP-08 终态转化率 | 周期内已转化在已转化和已流失终态中的占比。 | 分子：OP-07 的已转化 `lead_id` 数；分母：同周期首次已转化 `lead_id` 数 + 首次已流失 `lead_id` 数；分母为 0 显示 `--`。 | `lead_id` | 含已转化、已流失首次终态事件；排除非终态、无效接入、被合并源。 | `lead_stage_history.occurred_at` | 北京时间（UTC+8） | 城市、来源、团队、经纪人、楼盘、房源。 | 每 5 分钟；终态/合并事件后 60 秒内失效，日终 01:00 回算。 | 线索漏斗：终态为已转化或已流失、事件时间为当前周期。 |
| OP-09 活跃商户数 | 查询时刻拥有至少一条活跃房源的合格商户数。 | 分子：存在 OP-02 房源的商户数；分母：不适用。 | `merchant_id` | 含商户启用、资质通过且有效、可发布且有活跃房源；排除停用、资质过期、无活跃房源。 | `snapshot_at` | 北京时间（UTC+8） | 城市、商户类型、服务城市。 | 每 5 分钟；商户资格或房源事件后 60 秒内失效。 | 商户管理：`status=启用`、资质有效、关联活跃房源大于 0。 |
| OP-10 活跃经纪人数 | 周期内完成有效跟进或成功带看的合格经纪人数。 | 分子：有至少一条有效跟进或成功带看的合格经纪人数；分母：不适用。 | `broker_id` | 含事件时经纪人启用、双核验有效、主团队启用；排除停用、资格失效、测试和无效更正记录。 | `follow_up_record.created_at`，无跟进时 `viewing_record.completed_at` | 北京时间（UTC+8） | 城市、商户、团队、经纪人。 | 每 5 分钟；跟进/带看/资格事件后 60 秒内失效。 | 经纪人管理：当前范围、周期内有效活动大于 0。 |
| OP-11 经纪人活动率 | 可承接经纪人中周期内有有效客户活动者的比例。 | 分子：OP-10 中可承接经纪人数；分母：周期内任一时点具分配资格的经纪人数；分母为 0 显示 `--`。 | `broker_id` | 分子含有效跟进/成功带看；分母含启用、双核验有效、团队启用、服务范围有效、容量大于 0；排除停用或资格失效。 | 分子 `follow_up_record.created_at`/`viewing_record.completed_at`；分母 `broker_eligibility_snapshot.snapshot_at` | 北京时间（UTC+8） | 城市、商户、团队。 | 每 5 分钟；资格/活动事件后 60 秒内失效。 | 经纪人管理：显示分子“有活动”和分母“可承接”筛选。 |
| OP-12 团队转化排行 | 当前范围内团队按周期转化数排序的前 10 名。 | 分子：团队 OP-07 转化 `lead_id` 数；分母：不适用；同值按 `team_id` 升序。 | `team_id + lead_id` | 含转化事件时归属团队；排除无团队、已合并源、无权限团队。 | `lead_stage_history.occurred_at`（`converted_at`） | 北京时间（UTC+8） | 城市、商户、团队。 | 每 5 分钟；转化或归属历史事件后 60 秒内失效。 | 团队管理或线索池：团队 ID、`stage=已转化`、当前周期。 |

## 13. 埋点、通知与审计

埋点包括 `analytics_overview_view`、`analytics_filter_change`、`analytics_compare_change`、`analytics_metric_glossary_open`、`analytics_drilldown`、`analytics_refresh`、`analytics_export`，记录页面、指标 ID、查询快照 ID、范围和耗时。聚合延迟超过 30 分钟通知平台管理员；数据质量事件（缺失事件时间、无归属、重复键）通知数据运营。审计记录账号、角色、筛选、指标、导出字段、行数、结果、时间（秒）和请求 ID，不记录客户明文。

## 14. 验收标准

| 类别 | 可测试条件 |
|---|---|
| 正常 | 选择近 30 日后，OP-04 至 OP-08 的卡片、趋势和下钻使用同一北京时间周期且总数与明细去重结果一致。 |
| 正常 | 排行按 OP-07 降序、同值按 `team_id` 升序；切换城市、商户、团队后所有卡片同步重算。 |
| 权限 | 销售主管只能看到本团队，运营人员不能查看未授权城市；无指标权限组件不显示且导出无对应行。 |
| 异常 | OP-08 或 OP-11 分母为 0 显示 `--`；聚合延迟时显示最近成功时间并可重试，不显示伪造的 0。 |
| 数据 | 被合并源 `lead_id` 不重复计数；人员转组后流量按事件时归属、存量按查询时状态；已转化不被合同或支付事件二次计数。 |

## 15. 依赖与风险

依赖统一权限、组织与归属历史、房源发布/审核/暂停事件、线索阶段历史、带看和跟进记录、聚合任务与导出审计。风险是历史事件缺少时区或归属快照、带看记录未结构化、商户资格延迟及人工合并回算；上线前必须回填 `occurred_at`、归属历史和合并映射，并对指标快照和明细抽样对账。MVP 不使用前台曝光推断成交。

## 16. 截图参考

| 参考截图 | 采用能力 | 未采用能力 |
|---|---|---|
| 阿里商办列表页 | 楼盘/房源数量、面积、价格和区域筛选，映射为供给与区域趋势。 | 广告、热门推荐和营销承诺。 |
| 阿里商办详情页 | 楼盘可租房源、顾问与咨询，映射为供给、人员和线索归因。 | 消费者侧详情视觉。 |
| 58 商办列表页 | 租售、装修、位置、认证与咨询来源，映射为可切分的经营维度。 | 前台推广位。 |
| 58 商办详情页 | 房源顾问、咨询入口和在租状态，映射为带看与转化的业务输入。 | 交易闭环和相关推荐算法。 |
